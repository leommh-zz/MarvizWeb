import { REGISTER } from "../reducers/types";

export const goRegister = ({ username, password }) => {
    console.log('Registrou')
//   return (dispatch, getState) => {
//     // dispatch({ type: REGISTER, payload: response });
//   };
};
