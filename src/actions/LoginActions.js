import { LOGIN } from "../reducers/types";

export const goLogin = ({ username, password }) => {
    console.log('logou')
//   return (dispatch, getState) => {
//     // dispatch({ type: LOGIN, payload: response });
//   };
};
